CREATE TABLE wordListEntity (
    id INTEGER NOT NULL PRIMARY KEY,
    spanishWord TEXT NOT NULL,
    englishWord TEXT NOT NULL,
    audioPath TEXT
);

getWords:
SELECT *
FROM wordListEntity
ORDER BY id ASC;

getWordByID:
SELECT *
FROM wordListEntity
WHERE id = :id;
